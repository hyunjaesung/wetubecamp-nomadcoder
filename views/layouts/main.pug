doctype html
html
    head
        title #{pageTitle} | #{siteName}
    body
        header
            h1 #{siteName}
        main
            block content
        footer
            span &copy; Steve